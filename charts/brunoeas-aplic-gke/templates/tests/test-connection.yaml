# apiVersion: v1
# kind: Pod
# metadata:
#   name: "{{ include "brunoeas-aplic-gke.fullname" . }}-test-connection"
#   labels:
#     {{- include "brunoeas-aplic-gke.labels" . | nindent 4 }}
#   annotations:
#     "helm.sh/hook": test
# spec:
#   containers:
#     - name: wget
#       image: busybox
#       command: ['wget']
#       args: ['{{ include "brunoeas-aplic-gke.fullname" . }}:{{ .Values.service.port }}']
#   restartPolicy: Never
