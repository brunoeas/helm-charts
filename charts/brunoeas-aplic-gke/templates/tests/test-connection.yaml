apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brunoeas-aplic.fullname" . }}-test-connection"
  labels:
    {{- include "brunoeas-aplic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brunoeas-aplic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
